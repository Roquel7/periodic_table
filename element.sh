#!/bin/bash
if [[ -z $1 ]]
then
  echo "Please provide an element as an argument."
else
  # Join the proerties and the elements tables
  echo "this works!"
  # you can search the atomic number, symbol, or name. 

  # it will output the type, atomic mass, melting and boiling point.
fi
